## General project information
USERNAME="computational.genomics.bsc"
PUBLIC_SSH_FILE="${HOME}/.ssh/id_rsa.pub"
PROJECT_NAME="guidance"
IDENTIFICATION_JSON="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )/../guidance-252716-784e2a010688.json"

## Base instance information
BASE_INSTANCE_NAME_MASTER="instance${PROJECT_NAME}basemaster"
BASE_INSTANCE_NAME_WORKER="instance${PROJECT_NAME}baseworker"
OVERRIDE_INSTANCE="true"

## Bucket information
#BUCKET_NAME="bucket-${PROJECT_NAME}"
BUCKET_NAME="bucket-computational-genomics"

## Snapshot names
SNAPSHOT_NAME_MASTER="snap${PROJECT_NAME}master"
SNAPSHOT_NAME_WORKER="snap${PROJECT_NAME}worker"

## Cluster options
CLUSTER_INSTANCE_NAME="${PROJECT_NAME}cluster"
MASTER_DISK="500"
WORKER_DISK="40"
NODE_MEM="highmem"  # standard, highmem, highcpu
NODE_CPUS=96
NUM_NODES=30
